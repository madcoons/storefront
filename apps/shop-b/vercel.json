{
  "framework": "nuxtjs",
  "buildCommand": "npm run build",
  "installCommand": "cd ../.. && npm install",
  "ignoreCommand": "git diff HEAD^ HEAD --name-only | grep -qE '^apps/shop-b/|^packages/' && exit 1; exit 0"
}
