{
  "framework": "nuxtjs",
  "buildCommand": "npm run build",
  "installCommand": "cd ../.. && npm install",
  "ignoreCommand": "git diff HEAD^ HEAD --name-only | grep -qE '^apps/shop-a/|^packages/' && exit 1; exit 0"
}
